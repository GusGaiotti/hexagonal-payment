spring:
  application:
    name: wallet-service-test
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

rabbitmq:
  queue:
    name: wallet-debit-queue-test